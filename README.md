# kafka-search-analytics-service
Spring Boot / Java microservice that consumes all Kafka topic partitions in a single consumer group instance to aggregate search events into a PostgreSQL analytics store
